[build]
builder = "nixpacks"

[deploy]
startCommand = "./railway-start.sh"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

# Frontend service configuration
# This service will use Railway's PORT environment variable
